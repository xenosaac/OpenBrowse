{
  "name": "@openbrowse/scheduler",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "dependencies": {
    "@openbrowse/contracts": "workspace:*"
  },
  "scripts": {
    "build": "echo 'scheduler scaffold only'",
    "lint": "echo 'scheduler scaffold only'",
    "typecheck": "echo 'scheduler scaffold only'"
  }
}

